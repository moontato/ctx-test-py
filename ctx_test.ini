[ctx_test]
url       = http://127.0.0.1:8080
model     = my-model-alias

# Optional — these are the defaults if omitted
# prefill_mode = cold          # cold | incremental
# content_mode = text          # text | image+text | multi-image
# start        = 8000
# step         = 8000
# max          = 256000
# threshold    = 58.0
# timeout      = 1800
# image_width  = 448
# image_height = 448
